trigger Zenek_zlyTrigger on Club__c (before insert, before update, before delete) {
    /*
    Integer delay = 200000;
    Long start = DateTime.now().getTime();
    while(DateTime.now().getTime()-start<delay); 
    */
}